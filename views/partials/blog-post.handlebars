{{!-- This is the blog post partial --}}
<div class="mb-4 p-4 bg-white rounded shadow">
  <h2 class="text-xl font-bold">{{post.title}}</h2>
  <p>{{post.content}}</p>
  <div class="mt-4">
    {{#if post.comments}}
      {{> comments comments=post.comments}}
    {{/if}}
  </div>
</div>
