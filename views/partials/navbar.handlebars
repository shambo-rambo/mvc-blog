<nav class="bg-green-600 p-4">
  <div class="container mx-auto flex items-center justify-between">
    <div class="text-white text-lg font-bold">
      The Tech Blog
    </div>
    <div class="flex items-center">
      <a href="/" class="text-white px-4 py-2 hover:bg-green-700">Home</a>
      <a href="/dashboard" class="text-white px-4 py-2 hover:bg-green-700">Dashboard</a>
      <!-- Conditional rendering for authentication state -->
      {{#if loggedIn}}
        <a href="/logout" class="text-white px-4 py-2 hover:bg-green-700">Logout</a>
      {{else}}
        <a href="/login" class="text-white px-4 py-2 hover:bg-green-700">Login</a>
      {{/if}}
    </div>
  </div>
</nav>
